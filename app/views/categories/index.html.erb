
<div class="d-flex flex-row justify-content-around">
#<% categories = Category.includes(:most_recent_article).all%>
<% @categories.each do |category|%>
<div class=>
  <div class="">
    <h6 class=""><%= link_to category.name, category %></h6>
    <%= link_to image_tag(category.most_recent_article), category_path(category)%>
  </div>
</div>
<% end %>
</div>

